appId: ${APP_ID}
name: "Helper - Seed an ACTIVE booking (no-op)"
env:
  APP_ID: com.rentmy.app
---
# NO-OP: ACTIVE bookings are now seeded by setup.sh (SQL-based seeding).
# The backend test endpoint this helper originally called does not exist.
# Kept as a no-op so existing runFlow references don't break.
